# 机器学习基础 (2025) - 课程大作业 (第七组)

> **北京大学工学院 2025 年《机器学习基础》课程大作业代码仓库**

本仓库包含第七组关于专家模型复现、重训练以及 Router 模块训练的相关代码与数据集。

## 👥 小组成员
**石宇宸、龙想、田浩远、李宗远、石昊**

---

## 📂 目录结构说明

本仓库主要包含以下两个核心模块：

### 1. `test_originExpert` (官方模型复现)
用于验证和复现官方提供的专家模型性能。
* **Reproduce_Code**: 复现实验的核心代码。
* **Results_Origin**: 运行官方代码生成的原始输出数据。
* **Visualization**: 官方代码预测结果与原始数据集的对比可视化图表。

### 2. `train_ownExpert` (模型重训练)
本模块包含针对 Unet 和 PINN 模型的降维重训练代码。为了适配有限的计算资源并提升训练效率，我们对官方训练逻辑进行了深度优化。

#### 📐 模型输入维度配置
所有数据集在重训练时均采用均匀空间采样：
* **FNO 模型**: 采用分辨率自适应结构，**无需**重新训练。
    * `diff-sorp`: `[10 * 64]`
    * `advection`: `[10 * 64]`
    * `3d-cfd`: `[3 * 8 * 8 * 8]`
* **Unet 模型**: 在不同数据集下进行了降维重训练。
    * `diff-sorp` & `advection`: `[4 * 256]`
    * `3d-cfd`: `[3 * 8 * 8 * 8]`
* **PINN 模型**: 维度配置与 Unet 相同，并在 `Sample0` 上进行单样本物理泛化训练。

#### 🚀 核心优化与训练策略 (Core Optimizations)

相对于 PDEBench 官方实现，我们在复现过程中对训练管线进行了显著的**轻量化重构**：

**1. UNet 模型：训练策略变更 (Strategy Shift)**
* **官方实现 (Autoregressive Rollout)**：采用多步自回归展开，训练时递归预测 $t+1, \dots, t+N$。这导致计算图深度随步数线性增长，显存与时间开销巨大。
* **本项目优化 (One-Step Teacher Forcing)**：
    * 采用**单步监督策略**，始终使用真实历史数据 (Ground Truth) 预测下一帧。
    * **优势**：将计算图深度从 $O(N)$ 降至 $O(1)$，在大幅降低显存占用的同时（支持更大 Batch Size），避免了长时序训练的梯度累积误差，显著加速收敛。

**2. PINN 模型：工程架构优化 (Pipeline Optimization)**
* **官方实现**：依赖复杂的 PyTorch Dataset 封装与全量测试集评估，数据预处理与后处理耗时较长。
* **本项目优化 (Native Data Pipeline)**：
    * **原生数据流**：摒弃繁琐封装，使用 `h5py + NumPy` 直接对接 DeepXDE 框架，实现秒级数据加载。
    * **轻量化评估**：将训练后的全量 Metric 计算优化为**关键帧可视化 (Key-Frame Visualization)**，仅对 $t=\{50, 250, 450\}$ 等典型时刻进行推断，后处理效率提升 90% 以上。
    * **独立脚本**：解耦了对 PDEBench 库的复杂依赖，单脚本即可独立运行。

> ⚠️ **运行提示 (Note)**
> **如需跑通本仓库代码，需要对目录结构进行微调。**
> (建议下载解压后，根据代码中的相对路径索引检查并调整文件夹层级位置。)

## 🔗 资源下载 (Models & Datasets)

为了方便复现，我们将训练好的模型权重、生成数据集及 Router 数据集托管在 Hugging Face 社区，同时提供夸克云盘作为国内下载备用。

| 资源名称 | 内容说明 | Hugging Face 下载 | 夸克云盘下载 (备用) |
| :--- | :--- | :--- | :--- |
| **专家模型权重**<br>(Expert Models) | 包含 `ExpertModels_official` (官方模型) 与 `ExpertModels_own` (降维重训练模型及对比图)。 | [点击跳转](https://huggingface.co/CrisC196/ExpertModels) | [点击下载](https://pan.quark.cn/s/ea1805d61471) |
| **文生计算数据集**<br>(Text-to-Computation) | 包含用于训练文生模块的数据集及数据集生成代码。 | [点击跳转](https://huggingface.co/datasets/CrisC196/TEXT_to_Computation) | [点击下载](https://pan.quark.cn/s/09f16adf09d3) |
| **Router 数据集**<br>(Router Dataset) |包含用于训练Router的数据集及数据集生成代码。 | [点击跳转](https://huggingface.co/datasets/CrisC196/Router_dataset) | [点击下载](https://pan.quark.cn/s/468ffc7f6b1a) |

---
